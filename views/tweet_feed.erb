<!DOCTYPE HTML>
<html>
	<head>
		<title><%= username %>'s Tweets</title>
	</head>

	<body>

	<h4><%= flash %></h4>

	<% if session['username'] == nil %>
		<a href="/login"><button>Login</button></a>
	<% else %>
		<a href="/logout"><button>Logout</button></a>
	<% end %>

		<h3><%= username %>'s tweets:</h3>
		<% tweets.each do |tweet| %>
			<p><%= "[#{tweet[0]['username']}] #{tweet[1]}" %><br />
			<%= "#{tweet[0]['body']}" %></p>
		<% end %><br />

	</body>
</html>